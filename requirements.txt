# Core pipeline
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
tenacity>=8.2

# Email / contact validation
dnspython>=2.4

# RAG / API
fastapi>=0.110
uvicorn>=0.29
qdrant-client>=1.9
model2vec>=0.3          # local static embeddings (pure numpy; no torch/onnx)
pytest>=8.0
# note: sentence-transformers/torch/onnxruntime fail to load on Python 3.14
# (Windows DLL init error) — model2vec is the keyless local replacement.
