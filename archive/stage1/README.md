# Archived — Stage-1 deliverables (superseded)

These are the **Stage-1** files, kept only for history — **not** the current
product. The live Stage-2 dataset is the single file `data/final/dataset.csv`
(produced by the unattended climb; see `docs/DATA_HONEST_LIMITS.md`).

- `dataset.csv` / `dataset.xlsx` — the Stage-1 delivered records.
- `extended_qualified.csv` — Stage-1 qualifiers below the top-50 bar.
- `rejection_log.csv` — Stage-1 rejections.
- `stage1_requalification.csv` — the S8 audit: the Stage-1 pool re-run through the
  corrected Stage-2 engine (0/281 qualified under the new standard).

Do not treat anything in this folder as current Stage-2 output.
